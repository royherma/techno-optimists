{
  "name": "techno-optimists",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "wrangler dev",
    "dev:web": "npm --workspace web run dev",
    "build:web": "node scripts/build-web.mjs",
    "typecheck": "npm --workspace api run typecheck",
    "test": "npm --workspace api run test",
    "db:local": "wrangler d1 execute techno-optimists --local --file packages/db/schema.sql",
    "seed:gen": "node scripts/gen-seed.mjs",
    "db:seed:local": "npm run seed:gen && wrangler d1 execute techno-optimists --local --file packages/db/seed.sql",
    "db:reset:local": "npm run db:local && npm run db:seed:local",
    "build:web:only": "npm --workspace web run build",
    "deploy:dev": "npm run build:web && wrangler deploy --env dev",
    "deploy:prod": "npm run build:web && wrangler deploy --env prod",
    "db:prod": "wrangler d1 execute techno-optimists --remote --file packages/db/schema.sql",
    "db:dev": "wrangler d1 execute techno-optimists-dev --remote --file packages/db/schema.sql",
    "db:seed:dev": "npm run seed:gen && wrangler d1 execute techno-optimists-dev --remote --file packages/db/seed.sql",
    "db:reset:dev": "npm run db:dev && npm run db:seed:dev",
    "tail:dev": "wrangler tail --env dev",
    "auth:doctor": "node scripts/auth-doctor.mjs",
    "tail:prod": "wrangler tail --env prod",
    "verify": "node scripts/verify.mjs",
    "verify:dev": "node scripts/verify.mjs --base https://techno-optimists-dev.techguyver1337.workers.dev",
    "ship": "npm run typecheck && npm run db:local && npm run deploy:prod && npm run verify",
    "ship:dev": "npm run typecheck && npm run deploy:dev && npm run verify:dev"
  },
  "overrides": {
    "vite": "^8.2.2"
  },
  "devDependencies": {
    "wrangler": "^4.131.0"
  }
}
